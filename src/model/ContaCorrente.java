package model;

public class ContaCorrente extends Conta {

    public ContaCorrente(String numeroConta, Cliente cliente) {
        super(numeroConta, cliente);
    }

    // Pode adicionar métodos específicos depois (ex: taxas, limites)
}
